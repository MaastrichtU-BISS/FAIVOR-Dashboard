# *************************
#* Database               *
# *************************
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=faivor

# Auth
# Run with `npx auth secret` to generate a new secret
AUTH_SECRET="" # Added by `npx auth`. Read more: https://cli.authjs.dev

# Application Settings
PUBLIC_ORGANIZATION_NAME="FAIVOR"
NODE_ENV="development"

# Backend API Configuration
PUBLIC_FAIVOR_BACKEND_URL="http://localhost:8000"

# CORS Configuration (for reverse proxy deployments)
# Comma-separated list of allowed origins (e.g., "https://example.com,https://app.example.com")
# Set to "*" for development only (not recommended for production)
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"